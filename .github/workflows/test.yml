name: Deploy to DEV
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
jobs:
  Deploy-to-DEV:
    runs-on: ubuntu-latest
    steps:
    - name: CHECKOUT
      uses: actions/checkout@v3
      with:
        fetch-depth: 0
    - name: RUN COMMAND
      run: echo "DEPLOY TO DEV"
